# Add these only when connecting the approved local Order prototype to Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_TURNSTILE_SITE_KEY=

# Local Vite development uses the same-origin Worker by default.
VITE_API_BASE_URL=
